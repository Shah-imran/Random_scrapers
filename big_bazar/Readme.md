#### Run the python script "bigbazar_scraping.py" from termial. Before running the script, make sure you have all the modules installed mentioned in the "requirements.txt" file. Also note that browser "Chrome" is required to run the script. Please make sure your Chrome version is compatible with the driver. If chrome driver is currently not installed, please check you Chrome version and download the corresponding webdriver from https://chromedriver.chromium.org/downloads. The output file will be save as csv, onece the script is finished.  